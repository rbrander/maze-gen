{
  "name": "maze-gen-server",
  "version": "1.0.0",
  "description": "A web service for generating maze data",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rob Brander",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.2",
    "express": "^4.16.3"
  },
  "devDependencies": {}
}
